# RT SQL Studio - Configuração de Portas
# Copie este arquivo para .env na raiz do projeto e ajuste as portas conforme necessário

# Porta do Backend (API)
BACKEND_PORT=3001

# Porta do Frontend (Vite Dev Server)
FRONTEND_PORT=3000

# URL da API (usada pelo frontend)
# Se você mudar a porta do backend, atualize aqui também
VITE_API_URL=http://localhost:3001

# URL do WebSocket (usada pelo frontend para monitoramento)
# Se você mudar a porta do backend, atualize aqui também
VITE_WS_URL=ws://localhost:3001

